# Generated by scripts/package_capi.py from vanedb-capi/packaging/vanedbConfig.cmake.in.
#
# find_package(vanedb) entry point. Relocatable: every path is relative to
# this file, which lives three levels below the archive root at
# <root>/lib/cmake/vanedb. Point CMAKE_PREFIX_PATH (or vanedb_DIR) at the
# extracted archive and link vanedb::shared or vanedb::static.
get_filename_component(_vanedb_prefix "${CMAKE_CURRENT_LIST_DIR}/../../.." ABSOLUTE)

set(vanedb_VERSION "@VERSION@")
set(vanedb_ABI_VERSION @ABI_VERSION@)
set(vanedb_INCLUDE_DIRS "${_vanedb_prefix}/include")

include("${CMAKE_CURRENT_LIST_DIR}/vanedbTargets.cmake")

unset(_vanedb_prefix)
set(vanedb_FOUND TRUE)
