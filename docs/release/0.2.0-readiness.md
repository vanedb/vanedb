# 0.2.0 release readiness

Status: **not ready**. This record tracks the full accepted release scope;
it is not publication approval. Initial audit: 2026-09-21, main
`487d79b6121c18665f2d57865b5924a2e72395ab`.

Evidence snapshot: **2026-09-25**, engine main
[`6de2d3191cf30736976e9da7242a42f803d10971`](https://github.com/vanedb/vanedb/commit/6de2d3191cf30736976e9da7242a42f803d10971)
and demo main `5f98bfec`. This documentation branch has not yet incorporated
that engine main. Run states below are dated observations, not predictions.
Local combined candidates and reviewed PR heads are not final-source acceptance
or approval to merge, tag or publish. September 24 evidence remains historical.

The [roadmap](../ROADMAP.md), accepted [RFCs](../rfcs/README.md), and
[0.2.0 milestone](https://github.com/vanedb/vanedb/milestone/1) define scope.
Closed issues do not replace acceptance evidence. In particular, the
remaining benchmark/demo work is tracked by
[#198](https://github.com/vanedb/vanedb/issues/198) and
[#242](https://github.com/vanedb/vanedb/issues/242), both **open** at this checkpoint.

## Work and dependencies

| Order | Requirement | Current evidence | Remaining gate |
|---|---|---|---|
| 1 | Installed wheel verification (#252) | [PR #267](https://github.com/vanedb/vanedb/pull/267) merged as `e25ebe3` after current-head green checks; #252 closed. Original ten-role approval, nine wheel regressions and the independently reviewed depth-50 tag-fixture regression are retained | Final-source installed-wheel QA |
| 1 | Filtered search, RFC 0004 (#251) | #214/#263 and [PR #268](https://github.com/vanedb/vanedb/pull/268) merged; #268 merge `78d682c`, [current-head CI](https://github.com/vanedb/vanedb/actions/runs/36076382602) green. [Recall evidence](0.2.0-filtered-search-validation.md) reproduced all 1,000 queries; original ten-role reviews retained | Final-source binding/recall QA; reconcile dedicated-host wording with the existing quiet-host campaign |
| 1 | WebAssembly persistence, RFC 0006 (#248) | [PR #286](https://github.com/vanedb/vanedb/pull/286) merged as `1967eb1` after [current-head CI](https://github.com/vanedb/vanedb/actions/runs/36073281098); #248 closed. Ten-role-approved regressions cover WASM memory growth, real Node interruption, IndexedDB rollback and C buffers; earlier Chrome/WebKit macOS, Firefox Linux ARM64 VM and unsafe-artifact controls passed | Repeat final packaged browser reload, Node restart, cross-language bytes, corruption and package-size checks |
| 1 | API vocabulary and platform policy, RFCs 0011/0012 | #259/#256 merged; Metal presentation #255 merged | Final binding/type-declaration tests, migration notes and claims aligned with runtime evidence |
| 2 | C ABI distribution stage 1 (#193) | [PR #265](https://github.com/vanedb/vanedb/pull/265) merged as `d874df4` after current-head green checks. Integer handles and native packages retain all ten reviews; archive member name/order preservation resolves the Windows static-runtime crash, confirmed by native CI | Final-source ABI/export/header/lifecycle and extracted-consumer QA |
| 3 | C ABI distribution stage 5 (#197) | [PR #269](https://github.com/vanedb/vanedb/pull/269) merged as `6de2d319` after [current-head CI](https://github.com/vanedb/vanedb/actions/runs/36077481154). [Rehearsal 36077488648](https://github.com/vanedb/vanedb/actions/runs/36077488648) at `3ecae9c` passed all five native builds and signing/verification; all 13 retained signatures independently verified; both publication jobs skipped. Artifacts still identify 0.1.1 | Final-source signed 0.2.0 artifact inventory and identity verification |
| 3 | Write performance, RFC 0010 (#77/#109) | #264 merged; bulk-write [PR #293](https://github.com/vanedb/vanedb/pull/293) at `e3156d5` is reviewed but unmerged, with [current-head CI 36079203491](https://github.com/vanedb/vanedb/actions/runs/36079203491) pending. September 24 and 25 unreserved diagnostics are described below; neither is acceptance | Current-base green CI and merge; dedicated interleaved write rows within 1.1× C++, all search rows within the noise floor, refreshed snapshot/annotations or explicit measured RFC amendment |
| 4 | Competitor benchmark, RFC 0003 (#242) | Locked harness and pinned fixture available; full Mac cosine/L2 diagnostic runs completed without dedicated-host attestation | Six complete dedicated-hardware tables: cosine/L2 on Apple Silicon, Linux AVX2, Android ARM64; raw rounds, recall and caveats |
| 4 | Capacity research (#210) | #210 is closed as completed research; analysis/limits corrections merged in #254 and RFC 0008 amended. Completed research remains in 0.2.0. The [real-fixture allocator/link-density study](../research/capacity-fixture-probe/REPORT.md) adds a bounded sanity check | The approved split preserves the full physical-device resident/mapped/binary-plus-rescoring matrix in [#285](https://github.com/vanedb/vanedb/issues/285), milestone 0.4.0 after RFC 0005 in 0.3.0 |
| 5 | Demo and launch, RFC 0003 | Demo provider, centroid, endpoint and tooling fixes merged in #23/#21/#22/#24; release [PR #20](https://github.com/vanedb/obsidian-vane-search/pull/20) merged as `5f98bfec`. The [updated real Obsidian/Ollama acceptance and screenshot](https://github.com/vanedb/obsidian-vane-search/blob/5f98bfec5fc16dcef171bcde7dee4f669bbe55f9/docs/releases/0.2.0-desktop-acceptance.md) cover failed rebuild recovery, full restart, successful retry, edits and deletion. All 140 tests, ten independent reviews and four PR CI checks passed; the merged build matches the accepted bundle; merged-main CI and the tag-helper dry run pass | Approved annotated tagging via `maintainer_closeout_226.sh --tag --confirm-vault-walkthrough`; official release and links; measured launch copy and maintainer review/publication decision |
| 5 | Issue automation corrections | Reminder [PR #291](https://github.com/vanedb/vanedb/pull/291) and How-preservation [PR #292](https://github.com/vanedb/vanedb/pull/292) are independently reviewed but unmerged; local serial compositions preserve approved source and pass 25 reminder / 25 How mocked regressions | Actual-main integration, independent composition check and fresh required CI before each merge; keep benchmark and publication conditions intact |
| 6 | Candidate metadata and release rehearsal | Main still identifies 0.1.1. [PR #272](https://github.com/vanedb/vanedb/pull/272) remote head remains `6d34ec65`; local preparation is `270954a` (combined `5caf3f0` plus reviewed notes only), not pushed. Historical remote CI and original role approvals are retained | Integrate actual prior main merges, fresh exact-head review/CI; merge #272 then initial #271; freeze source S and complete final acceptance below |
| 7 | Full release QA (#253) | Historical and local preflight evidence below is not final sign-off; selected-path PR CI does not prove the full platform/artifact matrix | Frozen-S rebuilt artifacts, complete platform matrix, persistence conformance, independent unanimous reviews and green merged-main CI |

Implementation order follows dependencies. Independent checker/test work may
run alongside C ABI work; stage 5 consumes the settled stage 1 packages.
Performance measurements follow all performance-relevant changes and run
without competing builds.

## Historical isolated integration evidence — September 24

Branch `codex/020-integration-candidate` at
[`35758c6ccac0b9e657be8d80e84d1744378df120`](https://github.com/vanedb/vanedb/commit/35758c6ccac0b9e657be8d80e84d1744378df120)
combines wheel `9c07b063`, filter `2c3e91d`, C ABI `ea537295`, signing
`c4545558`, metadata `6d34ec65` and readiness `2aaa3c0`, based on the
initial-audit main above. These September 24 results are historical: this
isolated validation branch lacks subsequent runtime changes and is neither
merged main nor the official final release candidate.

Local checks pass: workspace formatting and Clippy, 371 Rust tests with disk
enabled, 98 C++ tests, 88 Python script checks, bench formatting/Clippy/tests
including release identity and cross-engine persistence, and all three
published-install README checks. These checks do not establish dedicated
performance results or complete installed-artifact/platform acceptance.

Publication-disabled rehearsals on that exact combined SHA:

| Artifact | Run | Snapshot result |
|---|---|---|
| Crate and signed C packages | [35915232431](https://github.com/vanedb/vanedb/actions/runs/35915232431) | Succeeded; crate verification, all five native C builds and signing/verification passed; release attachment and crates.io publication skipped |
| Python | [35915237295](https://github.com/vanedb/vanedb/actions/runs/35915237295) | Succeeded; wheel/sdist builds, installed tests and artifact validation passed; TestPyPI and PyPI publication skipped |
| npm | [35915241688](https://github.com/vanedb/vanedb/actions/runs/35915241688) | Succeeded; package build/consumption passed and npm publication skipped |

The full release remains **not ready**: dedicated current-engine performance
measurements, official demo publication, launch evidence,
final artifact QA and independent review, and green CI on merged main remain
outstanding. The later capacity matrix remains required in
[#285](https://github.com/vanedb/vanedb/issues/285); its approved milestone split
waives no current-engine benchmark gate.

## September 25 local preflight and artifact controls

Combined source `5caf3f0d4a2a419b01f0281e46830aa4e83a4f9e` incorporates
prepared metadata, signing, bulk disk writes and issue corrections. Local
`270954a72f70faa517b8d57bd3a6ff16c07f65e8` changes only the reviewed release
notes from that source; the following tests were run at **5caf3f0**, not rerun
at 270954a or final main. Runtime and build inputs are unchanged by that notes
delta. Neither local commit has been pushed as an omnibus candidate.

- 375 workspace Rust tests with disk (307 core, 4 doctests, 64 C ABI host
  tests), 12 release-identity/cross-engine format tests, 25 reminder, 25 How
  and 14 tag-gate regressions passed; Clippy, formatting and actionlint passed.
  Host-only wasm crates execute no browser tests in that Rust total.
- A fresh Mac ARM64 C archive passed extracted CMake and pkg-config shared/
  static consumers, ctypes, ABI/version/header and 58-symbol checks. Archive
  SHA-256: `470d86bd5e55877d095b95458adddcf44a2f68bebb64d2fbecfafa8f3fc0bf3b`.
  Local Rust 1.94.1 differs from release Rust 1.94.0; this local-platform
  artifact has no Sigstore release signature and is not the five-platform inventory.
- On the local macOS 27 host, SDK 27 Python builds fail import with a Mach-O
  LINKEDIT string-pool error. Candidate controls failed with Maturin 1.12.6
  and 1.15.0 and Rust 1.98.1 and 1.94.1. Pre-bulk source also failed with
  Rust 1.94.1 / Maturin 1.15.0 / SDK 27, so reverting bulk writes is not a
  demonstrated remedy. Selecting SDK 26.5 with the same
  candidate, Rust 1.94.1, Maturin 1.15.0 and linker passes all **240 installed
  Python tests plus 15 disk-format cases** for CPython 3.13 / Mac ARM64.
  Wheel SHA-256: `083af74e6a398463eda7db7284f2871cdce265a96a7d3c1827d4aa94efdbe3aa`.
  This does not repair SDK 27 or validate other wheel variants. Retain failed
  controls; install/test the unmodified final rehearsal's Mac wheel on this
  host, recording its SDK/toolchain, and resolve any artifact failure.
- The September 25 disk-only A-B-A-B-A-B diagnostic compared pre-bulk
  `5e6d90e` with `5caf3f0` on an **unreserved** Mac. The candidate's ratio of
  median Rust and C++ build-time estimates was **1.058×**; one pass was
  **1.115×**. Baseline Rust spread was 11.66%, exceeding the 11.09% observed
  median change; this establishes no reliable speedup. It measured neither
  store-add nor search rows and cannot close any dedicated performance gate.

Historical September 24 branch checkpoints remain useful provenance, not
current acceptance: main was `b471004`; wheel `77d4ba7` included the
62-commit/depth-50 fixture reproduction; filter `2c3e91d` had [CI 35915168385](https://github.com/vanedb/vanedb/actions/runs/35915168385);
C ABI `ea537295` had [CI 35914057950](https://github.com/vanedb/vanedb/actions/runs/35914057950);
signing `c4545558` had [rehearsal 35914314442](https://github.com/vanedb/vanedb/actions/runs/35914314442);
metadata `6d34ec65` had [CI 35698702124](https://github.com/vanedb/vanedb/actions/runs/35698702124).
Their earlier green results do not transfer to changed heads.

## September 24 audit and desktop follow-through

The demo acceptance used source `35b82e3` with published `@vanedb/wasm`
0.1.1, Obsidian 1.13.7 and Ollama 0.34.2. The rebuilt merged demo commit
`5f98bfec` produces the same 222 KB `main.js`, SHA-256
`db6f18a706e3650470adcd4961dc1ba62d7011775bd4e60e4438f1b1dfd78775`.
This is six-note synthetic-vault acceptance, not engine 0.2.0 desktop
acceptance or a large-vault performance claim. No official demo tag or release
has been published. [Merged-main CI](https://github.com/vanedb/obsidian-vane-search/actions/runs/36064325791)
and [CodeQL](https://github.com/vanedb/obsidian-vane-search/actions/runs/36064325382)
passed on `5f98bfec`. The reviewed tag-helper dry run also passed: an
independent clean clone passed 140 tests, type checking, release metadata and
build/size checks, and reproduced the accepted checksum. It created no public
tag or release; publication approval remains pending. The official demo's
required screenshot is already satisfied by the linked acceptance record.
No new walkthrough or screenshot is required for that unchanged accepted
bundle. Separate engine-0.2 compatibility follow-through is useful evidence,
not a new requirement for the official demo that embeds engine 0.1.1; changing
its bundle/dependency would require the established renewed acceptance.

Engine [PR #287](https://github.com/vanedb/vanedb/pull/287) merged the safe
demo release helper. It requires current reviewed-head merge eligibility,
merged-main CI, real test-job success and a build matching the recorded
accepted checksum before tagging. The fixture-only CI follow-up is in #267;
it does not waive those release checks.

The Mac diagnostics used isolated engine candidate `35758c6`, not final
merged main. Full 100k × 768 competitor runs used all 1,000 queries and four
rounds per metric; RFC 0010 used six full A-B-A-B-A-B passes against baseline
`51cb2d8`. The host was not reserved, so these observations cannot populate
`bench/COMPARISON.md` or close performance gates. That September 24 disk-build
ratio was 1.148× Rust/C++, above the 1.1× target; it needs quiet-host
confirmation. Do not directly compare it with the later diagnostic built
using a different Rust toolchain. Linux AVX2 and Android ARM64 runs remain
missing. RFC 0003
permits a physical Android device or an explicitly labelled ARM64 emulator;
physical-device capacity measurements belong to the later #285 matrix.

## Capacity scope decision — 2026-09-24

The maintainer approved splitting #210: completed capacity research and the
RFC 0008 amendment remain in 0.2.0. The complete unfinished physical-device
matrix is tracked in [#285](https://github.com/vanedb/vanedb/issues/285), in
0.4.0 alongside RFC 0008 (#203), after RFC 0005 quantization (#200) in 0.3.0.
It still requires resident memory, warm/cold p50/p99 and recall@10 for resident
f32, mapped f32 and binary navigation with f32 rescoring at 100k/1M and, where
hardware allows, 10M on a physical Android device and an NVMe laptop. The
20 ms p99 decision and IVF fallback are preserved. The allocator study does
not fulfill that matrix.

RFC 0003's current-engine competitor tables and RFC 0010's write/search
regression evidence remain required for 0.2.0. Closing the completed research
issue does not prove or waive those gates or the later device/mode experiment.

## Freeze the release source before final evidence

Finish the remaining source PRs (#293, #291, #292), then merge the reviewed
manual full-matrix CI entry point before metadata #272 and initial readiness
#271. Each change requires actual-current-main integration, exact-head review
and green required CI. The CI entry point is prepared locally at `13812bb`;
it is not yet merged. Finalize packaged documentation, headers, workflows,
dependencies and all shipping/acceptance inputs before freezing.
The existing #271 capacity probe archive contains code, not just prose; retain
its historical provenance and reviewed isolation from shipping packages.

Designate the actual initial-#271 protected-main merge commit **S**, recording
its full SHA and tree. Keep a dedicated rehearsal branch pointing **exactly**
to S throughout the campaign. Confirm S remains on protected main; selected
PR jobs do not substitute for complete relevant platform QA at S.

Dispatch `ci.yml` from the exact-S rehearsal branch after its manual entry
point is merged. Confirm the recorded source and reusable-workflow source
are S, all Rust, C++ and integration components ran successfully, and the
Required CI Gate passed. Normal path-selected CI may skip these components
for a readiness-only change; rerunning an older CI run retains its older
source and cannot establish exact-S acceptance. This uses the existing
platform matrix and does not replace the artifact rehearsals below.

Dispatch three existing workflows from that branch: `publish-crate.yml`
(which calls `publish-capi.yml`), `publish-rust.yml` with
`publish_testpypi=false`, and `publish-wasm.yml`. Verify each run's actual
source and reusable-workflow source is S and all publication jobs skip.
Complete the final artifacts, dedicated performance evidence and independent
reviews below against S.

Record those results in a narrow prose-only evidence follow-up PR, merged as
**D** after its own current-base review and required CI. D documents S; it is
not the tested release source. Verify D's complete diff changes no shipping,
packaging, workflow, lock/dependency, benchmark workload or acceptance input,
and compare actual package file lists rather than assuming all docs are
excluded. An intended shipping/acceptance fix requires a new candidate S2 and
appropriate fresh verification; reassess any newly discovered release-critical
failure. Later evidence-only main movement alone does not invalidate S.

Only after acceptance and explicit publication authorization may engine tags
`vanedb-v0.2.0`, `vanedb-crate-v0.2.0` and `vanedb-wasm-v0.2.0` point to **S**,
not D. Tag-run source claims must remain S; tag builds/signatures have their
own hashes and identities and must be verified. Frozen source does not promise
byte-identical rebuilds, and a branch C signature is not a release-tag signature.
No source S, release tag or publication authorization is assigned here.

## Final candidate evidence

Record source SHA, artifact checksums, commands, toolchains and run URLs.
The final audit must identify evidence for each row above and each accepted
RFC criterion, not merely a count of passing tests.

- All `AGENTS.md` local checks, complete platform CI, C++ reference and shared
  VNDB v1/v2 conformance, legacy readers and corruption checks.
- All 28 Python wheels (CPython 3.11–3.14 across the seven established target
  rows) plus sdist with installed tests; the actual Mac rehearsal wheel also
  checked on the affected macOS 27 host. One core crate and one installed npm
  tarball with declarations, packaged Chrome/Firefox/WebKit persistence/reload,
  Node restart, and all five extracted shared/static native C targets with
  ABI/export/header/lifecycle checks. Verify the complete 13 C payloads and
  13 signatures against source, workflow and branch/tag identity as applicable.
- Mobile evidence labelled simulator/emulator unless a physical-device run
  is actually recorded. Experimental Metal claims remain within the platform
  policy. No CUDA support claim.
- Dedicated current-engine performance evidence with interleaved rounds and
  honest measurement limits; CI timings are not release performance proof.
  Refresh benchmark documentation/annotations and measured launch copy with
  caveats and maintainer review. Preserve the completed capacity research and
  link the outstanding #285 device/mode matrix without claiming it has run.
- Independent CTO, AI PM, senior engineer, QA, TPM, analyst, SRE, DevOps,
  product-design and UX verdicts, with findings resolved on the current head.
  Merge only with unanimous agreement and green required CI; refresh branches
  whenever protected main changes.
- Publication-disabled exact-S branch dispatches of the three entry workflows
  above; the C release workflow is called by the crate workflow. Publication
  jobs must skip on dispatch. Review S/artifacts and D/document accuracy as
  separate evidence scopes.

No release tags or registry publication are part of this readiness record.
