# 0.1.0 release readiness

Status: **verified, awaiting registry setup**. The active target is the first
**0.1.0** release. The code and its evidence are ready; the three registry
prerequisites below are owner actions and no publication is authorized until
they are done.

**Candidate `e3c2635` is verified at the final head.** The four independent
assessments (CTO, AI PM, senior engineer, QA) have all approved; QA's earlier
BLOCK on "fully test" is lifted. What remains is owner action on the three
registries plus one edit that must land in the release commit, listed below.

The previous record certified `bf797d9`, which is retained further down as
historical evidence. It was superseded for a reason worth keeping visible: the
`sanitize` (AddressSanitizer) and `coverage` jobs did not exist in
`rust-ci.yml` at that revision, so the CI run it cited was a *narrower gate*
than the branch requires. "Passes CI" is a claim about a gate, and the gate is
versioned too — evidence should pin which checks ran, not only that they
passed.

### Verified at `e3c2635` on 2026-09-08

Full CI, [run 34256444457](https://github.com/vanedb/vanedb/actions/runs/34256444457) — success, including the two jobs absent from the previous record:

- Rust component / AddressSanitizer (unsafe paths) — success
- Rust component / Coverage — success
- C++ component / Sanitizer Checks (address) and (undefined) — success
- C++ component / Code Coverage — success

Publication-disabled rehearsals, dispatched from `main` per RELEASING.md step 4
(a branch, never a tag). **Every publication job reported `skipped`**, which is
what the `event_name == 'push'` gate exists to guarantee:

| Workflow | Run | Publication job |
|---|---|---|
| Publish vanedb crate | [34257290371](https://github.com/vanedb/vanedb/actions/runs/34257290371) | `Publish to crates.io` — skipped |
| Publish vanedb | [34257294149](https://github.com/vanedb/vanedb/actions/runs/34257294149) | `Publish vanedb to PyPI`, `… to TestPyPI` — skipped |
| Publish vanedb-wasm to npm | [34257297349](https://github.com/vanedb/vanedb/actions/runs/34257297349) | `Publish to npm` — skipped |

Artifacts downloaded from those runs: 28 wheels, one sdist, one crate, one npm
tarball. The crate and the npm tarball are pinned by hash below because they
are the two single-file artifacts a consumer fetches directly; the wheels are
verified by the rehearsal's own install-and-test step rather than pinned
individually.

```
af6b032f6cbdbaf5d45ca8dee6dc80bc7f8884326cd0766e95e402d80f044bf2  vanedb-0.1.0.crate
```

Local verification on macOS ARM64 at this commit: the Rust, benchmark, C++,
Python and wasm suites all pass, with `cargo fmt --check`, `clippy -D warnings`
under both `disk` and `disk,gpu-metal`, and `actionlint` clean.

Test counts are deliberately not recorded. Every previous version of this
document carried them and every one went stale — 153/14/92 became 167/18/97
became different numbers again within days, and each correction was itself
outdated before the next commit landed. A count is a fact about a moment, not about the release,
and it invites the reader to check a number instead of the thing the number
stood for. The CI run linked above is the durable evidence: it names which
checks ran, which is what actually matters and what the superseded record
failed to pin.

### Still required before tagging

1. ~~**crates.io**: verify an owner-approved email.~~ Done — `anton@tsvetkov.org`
   verified on account `tsvet01`. The first publish must still be a manual
   bootstrap (RFC 3691: a trusted publisher cannot be configured before an
   initial publish); revoke that token afterwards.
2. ~~**PyPI**: narrow the pending publisher to the `pypi` environment.~~ Done.
   The publisher is `vanedb/vanedb`, workflow `publish-rust.yml`, environment
   `pypi`. TestPyPI is a separate registry with its own publisher list; the
   `testpypi` job only runs on an explicit dispatch, never on a tag.
3. **npm**: claim the `vanedb` **organisation** (not a user scope — a user
   scope ties the package to one personal account and moving off it later is
   not clean). Then bootstrap-publish with
   `npm publish --access public`: scoped packages default to private, so
   without the flag the command fails on a free org and silently publishes a
   private package on a paid one, leaving a trusted publisher configured for
   something nobody can install. Then configure the publisher at
   `Packages → @vanedb/wasm → Settings → Trusted publishing` — repository
   `vanedb/vanedb`, workflow `publish-wasm.yml`, environment `npm` — and
   revoke the token. npm's trusted publishing is per package and the settings
   page does not exist until the package does; there is no equivalent of
   PyPI's pending publisher. This mirrors the crates.io bootstrap, and an
   earlier version of this record wrongly said otherwise.
4. **The packaged READMEs**: `vanedb/README.md` and `vanedb-py/README.md` are
   inside the `.crate` and all 28 wheels, so they must carry the published
   install form *before* the candidate is designated — the post-publication
   step that fixes every other guide structurally cannot reach them. See
   RELEASING.md step 5.

Registry prerequisites and final maintainer approval also remain open.
No publication is authorized by this record.

## Scope and compatibility

Ship the Rust engine and its Python, C and WebAssembly bindings, with runnable
user guides and verification of the advertised platform matrix. Keep the C++
reference and its cross-engine tests working. This candidate does not switch
the production engine during the ongoing language-strategy discussion.

The 0.x API and persistence formats may evolve in minor releases. Existing
format, kind and continuation identifiers must not be reinterpreted; new
encodings require new identifiers while retaining readers for existing files.
Older readers need not accept future formats. Both engines currently preserve
VNDB v1 disk and VNDB v2 graph files across load/save, including graph tombstones.
Future insertions may produce different topology. Legacy-reader and corruption
checks remain required; the version change is not permission to discard them.

Optional Metal compute is a Rust API for explicitly uploaded vectors, not
transparent acceleration of the indexes. CUDA is a required, high-priority
[follow-up after the initial release](../ROADMAP.md), with no assigned version
or date. Mobile acceptance uses simulators/emulators; physical-device acceptance
remains a follow-up. Neither CUDA nor physical-device success is claimed here.

## Candidate requirements

Record the final source revision and evidence below after integration. A green
older revision or a 1.0.0 archive does not establish 0.1.0 artifact readiness.

| Requirement | Evidence needed for this candidate |
|---|---|
| Integration and identity | Main and contributor PRs reconciled; core, Python, C ABI, WASM and C++ version declarations plus both lockfiles agree on 0.1.0; release-identity and required PR checks pass |
| Core and persistence | Rust workspace and benchmark checks; legacy, corruption, shared fixtures, graph continuation and cross-engine preservation pass; retain early Python row validation and fallible allocation |
| Native Rust and C | Linux x86-64/ARM64, macOS Intel/ARM64 and Windows x64 CI; extracted C archives pass consumer tests and include verified compatibility metadata and checksums |
| Python | Publication-disabled rehearsal tests all 28 CPython 3.11–3.14 wheels across Linux glibc/musl x86-64/ARM64, macOS Intel/ARM64 and Windows x64; source archive builds/rebuilds; installed artifacts report 0.1.0 |
| WebAssembly | Node and browser tests; packaged consumers in Node, Chrome, Firefox and WebKit; separate Node/web tarballs identify 0.1.0 and match their checksums |
| Mobile | iOS ARM64 build and simulator acceptance; Android ARM64/x86-64 builds and x86-64 emulator acceptance; reconcile Android ARM64/16 KiB evidence with the final implementation |
| Metal and performance | Reconcile source changes with existing hardware correctness and dedicated-hardware performance evidence; repeat affected checks, without extrapolating CI timings or unrelated hardware |
| User documentation and roles | Guides, migration policy and draft notes agree; record scoped CTO, AI PM, staff/senior engineering, QA, architecture and AI research conclusions, with unresolved findings addressed |
| Registries and authorization | PyPI environment restriction verified; crates.io email verified and initial-publication path prepared; maintainer approves final source, artifacts and publication |

## Existing evidence and remaining work

The [superseded 1.0.0 readiness record](1.0.0-readiness.md) retains earlier
platform results, hardware measurements, artifact hashes and their exact source
revisions. Its `4442d50` CI and `6005ac5` crate/Python rehearsals passed, but those
archives identify **1.0.0**. Preserve them as historical evidence. Do not rename
or relabel them as 0.1.0 artifacts.

Integration adds upstream graph fixtures and continuation tests, batch allocation
safeguards and version checks. Run the affected checks and publication-disabled
rehearsals on the integrated candidate. Carry forward older evidence only after
checking the relevant implementation, dependencies and packaging inputs for
changes; record that scope explicitly. Required PR-head checks still apply.

PyPI signed-in inspection on September 7, 2026 confirmed the existing `vanedb`
pending publisher for `vanedb/vanedb` and `publish-rust.yml`. It currently allows
any GitHub environment: restrict it to `pypi` and verify the saved setting.
Crates.io account `tsvet01` exists; adding and verifying an owner-approved email
remains pending. Initial crate publication requires the bootstrap path described
in the [publishing runbook](RELEASING.md), followed by trusted-publisher setup.
Final publication approval is pending.

Use the [draft notes](0.1.0-notes.md) for user-facing release content. Record the
candidate commit, run URLs, artifact identities/checksums and final role findings
here before seeking publication approval. Do not mark readiness complete until
those records cover every requirement above.

## Integrated candidate validation — September 7

Integrated main `bfe2f64` (PRs #147–#148) and PR #149 through `4ceae75`.
Local validation of the combined 0.1.0 code passes: Rust workspace tests and
clippy; the installed Python wheel's tests; the WASM tests in Node; the C++ suite;
benchmark workspace tests including cross-engine disk/graph preservation;
graph-format integration tests; documentation-reference and release-identity
checks; workflow lint. New canonical graph fixtures retain their distinct
header values; migration tests preserve the original legacy fixture parameters.

Scoped review covered CTO/TPM/product release alignment, staff engineering,
QA/architecture integration, and AI PM/research claims. Findings addressed:
0.1.0 identity and 0.x policy, preserved implemented C++ coverage, Python row
validation before fallible allocation, documentation checker fence/target bugs,
a graph-size regression test reaching the wrong guard, and overly broad seed
and construction-beam documentation. These are scoped reviews of this delta,
not new reviews of every unchanged implementation.

CPU search kernels, Metal runtime, benchmark workloads and third-party dependency
versions are unchanged from the previous candidate. Historical scoped CPU/Metal
measurements remain relevant to those paths. Python batch conversion/allocation
latency was not measured. Current 0.1.0 artifact results are recorded below;
the older 1.0.0 archives are retained separately as historical evidence.

## Historical evidence for `bf797d9` — superseded, do not tag against this

Retained because the commits that cite it must stay readable. The artifacts
described here were built from code that no longer exists, and the CI run
predates the ASan and coverage jobs. See the status note at the top.

Artifact candidate `bf797d95000799f20e3c725b5c2a8b2981da22e6` passed:

- [Platform CI](https://github.com/vanedb/vanedb/actions/runs/34152696195), including native consumers, browser consumers, iOS simulator and Android x86-64 emulator acceptance.
- [CodeQL](https://github.com/vanedb/vanedb/runs/101838393625).
- [Python rehearsal](https://github.com/vanedb/vanedb/actions/runs/34152714808): all 28 installed wheels, source build/rebuild and distribution validation. PyPI and TestPyPI publication skipped.
- [Crate rehearsal](https://github.com/vanedb/vanedb/actions/runs/34152712383): extracted-package verification passed; crates.io publication skipped.

PR merge commit `31e0ba0` incorporates main `270280c` with an identical file
tree. Rehearsal artifacts retain their actual source revision; later regenerated
archives must have their own identities and checksums reconciled.

Downloaded 0.1.0 distributions pass version, license and checksum checks:
28 wheels, one source distribution, the crate, five C archives and two WASM
tarballs. Wheel property declarations are verified. The downloaded macOS ARM64
CPython 3.13 wheel passes all 153 tests, the macOS C archive passes quickstart
and acceptance, and the Node package passes generated-JavaScript acceptance.
The crate SHA-256 is
`39632aa9080ec481946b6d2d561cc49a96a976354f3e2110ec1cdc24a19a972f`.

The downloaded Android ARM64 bundle records CI merge `ed44ee0` (parents main
`bfe2f64` and candidate `bf797d9`). It passes all three metric checks on an
Android 15 / API 35 ARM64 emulator with 16,384-byte pages. Both ELF files have
four load segments with compatible alignment. The disposable emulator, AVD and
private ADB server were removed. This is fresh 0.1.0 runtime evidence; it does
not establish physical-device or oldest-Android-version acceptance.

C archives record Linux glibc 2.34 and macOS load targets of 10.12 on Intel and
11.0 on ARM64; the oldest supported Windows version remains unverified. Runtime
acceptance covers the recorded CI hosts, not every OS version that can load
the binaries.

The durable local evidence bundle is
`/Users/anton/code/codex/vanedb-release-evidence/vanedb-0.1.0-evidence-bf797d9.tar.gz`.
Its SHA-256 is `bf8b43a278bb867fe1f696c55a44797026f704f4422557eeec23e082b8f9d32b`.
All 86 evidence files were hash-verified after archiving. The bundle preserves
distributions, per-artifact manifests, run metadata, local consumer logs and
the Android runtime procedure/provenance. Registry setup and final maintainer
authorization remain required; successful rehearsals did not publish anything.
