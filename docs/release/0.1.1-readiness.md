# 0.1.1 release readiness

Status: **verified at the commit that would be tagged** (`74235d7`).

**What 0.1.1 publishes.** Four artifacts: the `vanedb` crate on crates.io, the
`vanedb` package on PyPI, `@vanedb/wasm` on npm, and the C ABI archives
attached to the release. The supplementary `vanedb-cpp` C++ package is
version-stamped 0.1.1 because `release_identity` requires every site to agree,
and is **not published anywhere** — it is reference code and the second arm of
the benchmark comparison. CI builds and tests it; no workflow uploads it.
Rust, Python, C and WebAssembly use **0.1.1**. PR #184 includes #185's Wasm
`upsert` and per-query beam width, and reconciles #186's C++ pickle fix.
Nothing is published; publication remains a separate maintainer decision.

### Verification at `74235d7`

Earlier evidence was produced at `30c1824` and `89e2780`, both of which predate
the eight-conflict merge with main and therefore predate the shipping Wasm
`upsert` and per-query beam width. A record that does not cover the code being
tagged is not a sign-off, so all three publication-disabled rehearsals were
re-dispatched at this head and the manifest regenerated from their artifacts.

| Rehearsal | Run | Result |
|---|---|---|
| Publish vanedb crate | [34474829075](https://github.com/vanedb/vanedb/actions/runs/34474829075) | success; crates.io job skipped |
| Publish vanedb (Python) | [34474835144](https://github.com/vanedb/vanedb/actions/runs/34474835144) | success; 28 wheels and the sdist built, installed and tested across CPython 3.11-3.14 on manylinux, musllinux, macOS and Windows; TestPyPI and PyPI jobs skipped |
| Publish vanedb-wasm to npm | [34474832268](https://github.com/vanedb/vanedb/actions/runs/34474832268) | success; npm job skipped |
| CI, source of the C ABI archives | [34474800750](https://github.com/vanedb/vanedb/actions/runs/34474800750) | success; Required CI Gate green, 62/62 checks |

A `workflow_dispatch` cannot satisfy the publish jobs' `event_name == 'push'`
condition, which is why each reports `skipped` rather than being disabled by
hand for the rehearsal.

The commit that records this evidence necessarily sits one above the commit the
evidence was produced at, so the tagged head is not literally `74235d7`. The
difference is checkable and must stay empty of shipping code:
`git diff 74235d7 HEAD --stat` should list only this file and the manifest. If
it lists anything else, the rehearsals no longer cover what would be tagged and
have to be re-dispatched.

## Candidate and review scope

[Release PR #184](https://github.com/vanedb/vanedb/pull/184) includes main
`65bb39b` through merge `9b53115`, incorporating PRs #185, #186 and #187.
The conflict-resolution audit preserved the constructor-based C++ reducer,
all unnamed-value cases, explicit integer-value and installed-version
assertions, and the installed-package tests. It also retained #187's stronger
Wasm beam-width regression, which detects an ignored per-query argument.

This replaces the superseded split-version plan in closed PR #183. The C++
engine remains reference code: its defects are fixed and tested, but its
Python package is not a shipping distribution.

Before final sign-off:

- Refresh full CI, publication-disabled artifact rehearsals, installed Node and
  browser checks, and independent reviews for the combined candidate.
- Preserve fresh artifacts with their exact source identities and checksums;
  update the evidence and role verdicts below after verification completes.
- PRs #185, #186 and #187 are merged into main and reconciled in #184.
  Require the release PR's current-head checks before merging it. Tag only
  the approved main commit after publication authorization.

No separate `is_empty` binding is planned: Python uses `len(index)` or truth
testing; WebAssembly uses `index.size()`.

Resolved release findings preserve the first Python batch-conversion error,
make metric hashing/pickle round trips correct, retain wasm runtime/type
validation, and publish the exact tested npm tarball. Onboarding now handles
provider query/document prefixes and states actual API limits. Benchmark
reports no longer attach fixed historical statistics to unrelated workloads.
Version declarations and active release documentation agree. These fixes
change neither the search algorithms nor the persistence formats.

## Prior candidate evidence — `30c1824`

The four implementation runs identify source `30c1824`; **all publication jobs
skipped**. Main-integration CI is recorded separately in the table.
CI uses merge `d8c0b9413d795a20aacd796555aaf370c306d5b8`, whose tree is
identical to that candidate, with parents main `69a994a` and `30c1824`.

| Requirement | Result |
|---|---|
| Full platform CI | [PASS, including Required CI Gate](https://github.com/vanedb/vanedb/actions/runs/34464028573): native Rust/C, C++ reference, mobile, browsers, sanitizers, cross-engine conformance, lint and MSRV |
| Main integration | [PASS, all 53 CI jobs](https://github.com/vanedb/vanedb/actions/runs/34465617408) at `d023929`: only README and release evidence differ from the tested implementation |
| Python 0.1.1 | [PASS](https://github.com/vanedb/vanedb/actions/runs/34463974061): all 28 installed CPython 3.11–3.14 wheels, sdist rebuild and metadata verification |
| Rust crate 0.1.1 | [PASS](https://github.com/vanedb/vanedb/actions/runs/34463971083): extracted-package build/tests, contents and exact VCS identity |
| npm 0.1.1 | [PASS](https://github.com/vanedb/vanedb/actions/runs/34463977235): preserved tarball, Node consumer and checksum verification; the downloaded tarball also passed Chrome, Firefox and WebKit locally |
| C and separate wasm archives | PASS: all five extracted C consumers, compatibility metadata, Node/browser packages and checksums in the full CI run |
| Local installed Python artifacts | PASS: 183 tests against the downloaded canonical macOS ARM64 wheel; 71 against the C++ reference wheel, including every pickle protocol |
| Android ARM64 / 16 KiB | PASS: the downloaded CI executable/library ran on Android 15 API 35, arm64-v8a, 16,384-byte pages; both ELF checks and L2/Cosine/Dot C ABI acceptance passed |
| Metal | PASS: local Apple hardware, `cargo test -p vanedb --features disk,gpu-metal --locked` |
| Release tooling | PASS: release identity, version-bump suite, 33 release-script cases, README validation, formatting and workflow lint |

The [artifact manifest](0.1.1-artifacts.sha256) covers the **36 distributions
that publish**: 28 wheels, one sdist, one crate, five C archives and the npm
tarball. Every path carries its source commit as a prefix, so a filename cannot
stand for two different artifacts. Each hash was checked against the sidecar
`.sha256` files and `SHA256SUMS` that CI published beside the archives, so the
manifest attests to the bytes CI produced rather than to a local rebuild.

The earlier count of 38 also counted the two intermediate wasm build
directories, which are inputs to the npm tarball rather than distributions.

The old 0.1.0 manifest is historical but is **not** disjoint from this one: its
first line records `4aa60c8/npm/vanedb-wasm-0.1.1.tgz`, from the abandoned plan
that proposed npm 0.1.1 alongside crates.io and PyPI 0.1.0. Two different
byte-sets therefore carry the same filename across the two manifests, for a
version npm will never allow to be re-uploaded. The 0.1.0 entry is superseded
and must not be published. The current manifest remains relative to the
`30c1824` evidence directory named above; its paths have not been rewritten.
Identify an artifact by its source revision and SHA-256, not its filename.
Record replacement evidence separately before updating this manifest.

## Prior candidate verdicts — `30c1824`

Reviewers inspected the implementation independently, rechecked their resolved
findings and reconciled the final artifact evidence. These verdicts approved the prior source
without open release-blocking findings; the expanded Wasm APIs require the
final review above.

| Role | Verdict | Focus |
|---|---|---|
| CRO | PASS | Adoption, positioning, onboarding and evidence-backed user promises |
| CTO | PASS | Release scope, simplicity and delivery risks |
| Architect | PASS | API boundaries, persistence contracts and artifact identity |
| Staff engineer | PASS | Implementation integrity, binding regressions and mobile acceptance |
| Senior engineer | PASS | Compact code, duplicate conversion/remapping removal and regression coverage |
| QA | PASS | Platform matrix, installed/extracted artifacts and failure paths |
| AI product manager | PASS | Onboarding, capability limits and unified version story |
| AI researcher | PASS | Embedding-provider semantics, retrieval claims and evaluation scope |
| TPM, SRE and analyst | PASS | Execution evidence, publication controls and measurement claims |

## Platform and compatibility scope

Runtime coverage includes Linux x86-64/ARM64, macOS Intel/ARM64 and Windows
x64. Python wheels additionally cover musl Linux. C archives record glibc
requirements and macOS deployment targets; those are binary requirements,
not proof of runtime behavior on the oldest compatible OS.

Mobile acceptance is simulator/emulator based: iOS ARM64, Android x86-64,
and Android ARM64 with 16 KiB pages. The recorded ARM64 runtime result used
the CI-built binary from `30c1824`. Its native Rust/C implementation, third-party
dependencies and Android build inputs are unchanged at `9b53115`; this supports
carrying forward that runtime result, not claiming a new binary was executed.
Current CI must still pass the mobile builds and acceptance jobs. Physical-device
and oldest-OS runtime acceptance remain follow-ups; no physical iPhone/Android
test is claimed.

Metal is an explicit Rust compute API; it does not automatically accelerate
indexes. CUDA remains a required [post-release follow-up](../ROADMAP.md).
Earlier dedicated-hardware performance evidence applies only to unchanged
implementations; CI timings are not performance evidence. The release delta
keeps third-party dependency versions unchanged.

VNDB v1 disk and VNDB v2 graph formats retain cross-engine conformance and
legacy readers. During 0.x, minor releases may change APIs/formats; existing
format identifiers are never reinterpreted. See the [release notes](0.1.1-notes.md).

## Publication handoff

No release tags, registry uploads or merges into main were performed by this sign-off.
Keep the current-head required checks green and use [RELEASING.md](RELEASING.md)
for protected-main integration, maintainer-approved publication and subsequent
published-install verification. These rehearsed artifacts have their own
recorded source/checksums; do not silently substitute a later rebuild.

All three trusted publishers executed successfully for rc.2. npm 0.1.0 was
already consumed by a manual bootstrap; using 0.1.1 for every registry removes
the earlier version split. TestPyPI 0.1.0 and 0.1.0rc1 contain partial historical
uploads. The 0.1.1 rehearsals above kept publication disabled.
