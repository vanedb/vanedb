# 1.0.0 release readiness

Status: **not ready to tag**. This record tracks evidence and remaining work;
it does not change the release scope or declare support from build checks alone.

## Release scope

Ship the Rust engine, Python package, C ABI and WebAssembly bindings with a
stable public contract, compact maintainable code, runnable user documentation,
and functional verification on every advertised platform. C++ remains frozen
reference code; keep its defect tests and cross-engine fixtures working.

## Required evidence

| Requirement | Evidence available | Still required |
|---|---|---|
| Core correctness and simple implementation | Native regression tests for malformed graphs, duplicate disk IDs, finite-first HNSW ordering; bounded top-k preserved | Final review of the release commit; dedicated-hardware recall/performance comparison after search changes |
| Stable public persistence | VNDB v1 disk specification and golden/cross-engine fixtures; legacy graph loaders tested | VNDB graph specification, implementation, golden files, faithful cross-load preservation, migration guidance |
| Native desktop/server behavior | Existing CI runs Linux x86-64, macOS ARM64, Windows x86-64; integration matrix adds Linux ARM64 and Intel macOS | Green combined-commit matrix and artifact acceptance |
| Python distributions | Release matrix builds 28 wheels across Python 3.11–3.14, glibc/musl x86-64/ARM64, macOS Intel/ARM64, Windows x64; local installed-wheel suite passes | Combined-commit 28-wheel rehearsal and source-distribution verification; final 1.0.0 metadata and registry readiness |
| C consumers | Native C header compilation and ABI tests; per-call beam-width regression; iOS ARM64 core and C ABI build passed locally | Consumer-link/runtime acceptance for release artifacts on all advertised native targets |
| WebAssembly | 12 tests pass in Node and headless Chrome; generated JavaScript test rejects wrapping BigInt IDs | Other advertised browser runtimes, artifact packaging and a complete consumer example |
| Mobile | iOS ARM64 and Android ARM64/x86-64 core/C ABI cross-compilation wired into CI | Simulator/emulator/device runtime acceptance, including persistence and search |
| GPU features | Native Metal suite passes with GPU access outside the local sandbox | Final-head Metal verification; resolve the advertised CUDA feature's build/runtime support with real hardware evidence |
| User documentation | Runnable Rust/Python source quickstarts, correct binding shapes, frozen C++ scope, honest persistence guidance | Final API/docs consistency audit, C/JS consumer guides, registry install commands after verified publication availability |
| Release operations | Product-scoped Python/crate tags and publication-disabled rehearsals; C++ publication removed | Final version alignment, current-head release artifacts and checks, release notes, distribution/credential readiness; no publication performed |
| Role review | Discovery covered product/AI PM/CRO, engineering/architecture, QA/SRE, and research concerns | Final role verdicts against the complete release candidate; discovery agents ended before final verification |

## Integration and concurrent work

[Draft PR #135](https://github.com/vanedb/vanedb/pull/135) combines this thread's
fixes with #129–#134 for validation. The original PRs remain independently
owned. Local integration is not approval or a GitHub merge of those PRs.
Refresh PR heads and main before edits, commits, pushes, and final release audits.
Read-only monitoring is scheduled every 15 minutes in the release thread.

Rehearsals started for integration commit `e169e6a`:

- [CI](https://github.com/vanedb/vanedb/actions/runs/34060250657)
- [Python artifacts, publication disabled](https://github.com/vanedb/vanedb/actions/runs/34060289510)
- [Rust crate dry run](https://github.com/vanedb/vanedb/actions/runs/34060291187)

These are live-run references, not claims of success. Later integrations require
new current-commit evidence before release.

Local checks on `e169e6a`: Rust workspace tests with `disk,gpu-metal`, benchmark
harness clippy/tests, 61 installed Python-wheel tests, and generated Node package
boundary tests passed. Earlier component checks also passed 87 C++ test cases,
12 WASM tests each in Node/Chrome, and the iOS ARM64 C ABI build. The #134 merge
at `f0f79f7` passed all 23 Rust corruption tests; broader checks are pending.
