# Safe calibration correction — independent Analyst review

**Findings: Clean — no blocking or actionable Analyst finding.** The self-test correction preserves the completed study's results and interpretation.

The historical ANALYST-REVIEW.md applies to original measured probe source `de8db76c0140f2266a432bd6b50d143b8dcbd9649a01433e7eb877913a3a4491` and binary `086304919cd32dc8a102015d517210cefc8d958a2202083f99f88ef878effab0`, preserved in the original task workspace under `historical-before-safe-calibration/`. This review independently verifies replacement probe source `469a9a7ff25ef4405ce54b80aba0affc2f2e731601ff3fa138bfb2d6f8448d08` and binary `1902809276275ac676ffa2a9505561cce3576acacdb7b4e4700609d2f4bc18c3`, and the nine completed reruns retained there in `results-safe-calibration/`. The archive may promote these current results into its canonical `results/` directory; the hashes, not directory labels, identify the campaign.

- A direct source boundary comparison confirms that only `self_test()` changed. The counting GlobalAlloc implementation, fixture parsing, measurement lifecycle, engine calls and output formulas are byte-identical outside that function. The replacement uses safe Vec reserve/resize/truncate/shrink/drop and snapshots of HEAP, plus null-result accounting simulation; it no longer performs raw-pointer writes/reads in the self-test. Allocation data preservation, zero initialization, grow/shrink and deallocation checks remain meaningful. I independently executed the new self-test binary successfully, without rebuilding it.
- Rehashed both old and new source/manifests against their corresponding metadata, and the new executable against its recorded binary hash. All match. Engine candidate remains clean at `35758c6ccac0b9e657be8d80e84d1744378df120`; both retained lockfiles are byte-identical across campaigns. Cargo metadata differs only because the scratch package now has a README (`readme: null` became `README.md`); the resolved dependency graph and engine package identities are unchanged. The fixture/source identity and analytical scope did not change.
- Independently compared every field in all nine old/new result objects. All deltas, post-drop residuals, released bytes, calibration bytes/capacities, size/configuration fields and fixture/source identities are identical. The five raw live snapshots in each case are exactly 17 B higher. Commands differ only in the output filename argument, which is 17 characters longer; that argument stays live through all snapshots. PIDs and output paths change as expected. This common baseline increase cancels from every reported measured delta.
- Independently compared all six file-verification objects, excluding only the output pathname. Every graph geometry value, degree histogram, invariant result, file length and SHA-256 is identical. Rehashed each of the six newly generated VNDB files and verified equality to both its new record and the historically independently checked file hash. Therefore the prior independent geometry/fixture-bit conclusions apply to identical new file bytes; no new geometry inference is needed.
- The complete marker records all three kinds at all three counts. At 100k the results remain 342,180,848 B released with the index, 262,144 B retained TLS, 31,148,320 B inferred neighbor allocation (311.4832 B/node), 26,901,424 B exact-fit neighbor geometry and mean layer-0 degree 26.38885. These source-derived values, lower-bound interpretation and uncontrolled historical-comparison caveats remain unchanged.
- No CodeQL result was suppressed or dismissed as part of this review; hosted CodeQL success is a separate check, not established by this bounded local analysis. The allocator's required unsafe trait implementation is unchanged; removing raw-pointer self-test operations does not assert that all code is safe Rust.

**VERDICT: PASS — safe-calibration source/binary and completed rerun evidence at the exact SHA-256 identities above.** No full graph run was performed by this reviewer. The prior raw-source/binary identities remain historical; they are not silently substituted for the new measured executable. Requested-heap/geometry scope only: no timing, RSS, device-capacity, release approval or gate waiver follows.
