# Recorded runs

JSON reports from dedicated-hardware runs go here (gitignored). Name them
`{hardware_label}-{YYYYMMDD}.json` and paste the matching markdown section into
[`../../COMPARISON.md`](../../COMPARISON.md).
