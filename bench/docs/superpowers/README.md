# Historical benchmark design archive

The plans and specifications below record how the benchmark harness was first
built across three repositories. They intentionally retain obsolete Git
dependency and submodule commands as historical evidence; do not execute those
instructions against the monorepo.

Current build and contribution instructions live in [`../../README.md`](../../README.md)
and the repository-root [`AGENTS.md`](../../../AGENTS.md).
